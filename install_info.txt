{pip install Flask-SQLAlchemy Flask-Migrate psycopg2-binary
pip install mysqlclient
pip install pymysql
pip install virtualenv(virtualenv venv, win:venv\Scripts\activate, mac:source venv/bin/activate)}

pip install -r requirements.txt





 # we using MySQL db (https://dev.mysql.com/downloads/mysql/) to download 9.0.1
